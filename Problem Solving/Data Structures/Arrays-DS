#include <assert.h>
#include <limits.h>
#include <math.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int i,n,j,k;
scanf("%d",&n);

int arr[n];
int rev[n];
j=n-1;
for(i=0;i<n;i++)
{
    scanf("%d",&arr[i]);
}
for(i=0;i<n;i++)
{
    rev[j]=arr[i];
    
    j--;
}
for(k=0;k<n;k++)
printf("%d ",rev[k]);


return (0);
}

